{
  "name": "weblab1",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint . --ignore-path .gitignore",
    "lint:fix": "npm run lint -- --fix --ignore-path .gitignore",
    "format": "npx prettier . --ignore-path .gitignore",
    "format:fix": "npx prettier --write . --ignore-path .gitignore",
    "lint:style": "stylelint \"src/**/*.css\" --ignore-path .gitignore",
    "lint:style:fix": "stylelint \"src/**/*.css\" --fix --ignore-path .gitignore",
    "prepare": "husky install"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/GrigoriyUstymenko/web-lab1.git"
  },
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/GrigoriyUstymenko/web-lab1/issues"
  },
  "homepage": "https://github.com/GrigoriyUstymenko/web-lab1#readme",
  "devDependencies": {
    "eslint": "^7.32.0",
    "husky": "^7.0.2",
    "prettier": "^2.4.0",
    "stylelint": "^13.13.1"
  }
}
